{
  "name": "yarabank",
  "version": "1.0.0",
  "description": "Projeto banco comunitário Reprograma",
  "main": "index.ts",
  "scripts": {
      "build": "tsc",
      "start": "tsc && node build/index.js",
      "dev": "ts-node-dev --ignore-watch node_modules src/index.ts",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
